<!DOCTYPE html>
<html lang="uk">
<head>
  <title>Помилка</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding-top: 40px;
      padding-bottom: 40px;
      background-color: #f5f5f5;
    }
    .error-container {
      max-width: 600px;
      padding: 15px;
      margin: 0 auto;
      text-align: center;
    }
    .error-icon {
      font-size: 3rem;
      color: #dc3545;
      margin-bottom: 1rem;
    }
    .btn-large {
      padding: 10px 20px;
      font-size: 16px;
      margin-top: 20px;
    }
    pre {
      text-align: left;
      background-color: #f8f9fa;
      padding: 15px;
      border-radius: 5px;
      max-height: 200px;
      overflow-y: auto;
      margin-top: 20px;
    }
    h1 {
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-container">
      <div class="error-icon">⚠️</div>
      <h1>Сталася помилка</h1>
      
      <div class="alert alert-danger">
        <% if (typeof error === 'string') { %>
          <%= error %>
        <% } else if (error && error.message) { %>
          <%= error.message %>
        <% } else { %>
          Невідома помилка
        <% } %>
      </div>
      
      <% if (error && error.stack) { %>
        <pre><%= error.stack %></pre>
      <% } %>
      
      <div class="d-flex justify-content-center">
        <a href="/admin" class="btn btn-primary btn-large me-2">На головну</a>
        <button onclick="history.back()" class="btn btn-secondary btn-large">Назад</button>
      </div>
    </div>
  </div>
</body>
</html>
